import styled from 'styled-components';

export const Title = styled.h2`
  color: ${p => p.theme.colors.colorText};
  margin-bottom: 10px;
  margin-top: 0;
  text-align: center;
  font-size: 34px;
`;
